#include<bits/stdc++.h>

using namespace std;

long reciept(string n){
    int max= -1, ind; long len= n.length(), dig= 0;
    while(dig< len){
        if(n[dig]>)
    }
}

int main(void){

    int t; cin >> t;
    while(t--){
        string n; cin >> n;
        reciept(n);
    }
    return 0;
}