#include <bits/stdc++.h>

using namespace std;

int extended_gcd(int a, int b, int *x, int *y)
{
    if (a == 0)
    {

        return b;
    }
}

int main(void)
{

    int a, b, x, y;
    cin >> a >> b;
    extended_gcd(a, b, &x, &y);

    return 0;
}